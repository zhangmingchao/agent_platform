# 2026-09-10 开发记录

## 删除旧工作流执行链并收敛 LangGraph State

### work_list

1. 在 `langchain_test_langgraph` 分支上确认原生动态图已覆盖线性、条件、并行和审批节点。
2. 删除旧 `_walk_graph()` 和 `_execute_dag()`，避免两套工作流引擎并存。
3. 删除旧 `_run_workflow_segment()` 和 `build_workflow_lifecycle_graph()` 生命周期包装。
4. 将工作流测试迁移到原生 `NativeWorkflowEngine` 测试。
5. 收敛 `WorkflowRuntimeState` 与 `NativeWorkflowState`，只保留原生动态 State。
6. 保留 `workflow_graph.py` 作为配置解析、校验和图结构查询工具。
7. 更新 State、Checkpoint 和迁移边界文档。
8. 执行语法检查、专项测试和后端完整回归测试。

### 实现结果

- 旧执行链已从源码和测试中删除。
- `workflow_service.py` 不再引用旧生命周期图或旧 DAG 遍历器。
- 统一使用 `NativeWorkflowState` 和 `NativeWorkflowEngine`。
- Checkpoint 继续使用 `workflow_run_v2_{run_id}` 命名空间。
- Python 语法检查、专项测试和后端完整 64 个测试全部通过。

## 本轮优化

- 删除 `workflow_service.py` 中遗留的 `_execute_sequential()` 旧顺序执行入口及无效 DAG 导入。
- `NativeWorkflowState` 增加 `state_version` 字段，首次运行状态写入版本 `1`，为后续 State 迁移预留边界。
- 增加基于稳定 JSON 配置键的拓扑标准化缓存（最多 128 项）；不缓存绑定运行上下文的编译图，避免跨运行污染 publisher、用户和回调。
- 验证结果：原生工作流、审批和类型标注专项测试 9 个通过；后端完整测试 64 个通过；`compileall` 与 `git diff --check` 通过。
