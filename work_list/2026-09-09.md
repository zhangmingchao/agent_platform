# 2026-09-09 开发记录

## 工作流模块拆分、实体化与 Checkpointer 强约束

### work_list

1. 将配置解析、步骤校验和 DAG 图结构查询从 `workflow_service.py` 提取到独立核心模块。
2. 将工作流定义 CRUD 提取到独立 Service，将数据库查询提取到 Repository。
3. 新增 `WorkflowDefinition`、`WorkflowRun`、`WorkflowResumeContext` 强类型实体。
4. 将运行恢复、人工审批和配置快照读取迁移为实体属性访问。
5. 保持现有 HTTP JSON 字段兼容，只在接口边界转换为字典。
6. 删除 Redis Checkpointer 的 `InMemorySaver` 回退逻辑，所有环境初始化失败都拒绝启动。
7. 增加实体转换、Repository 返回类型和 Checkpointer 强失败测试。
8. 执行 Python 语法检查、返回类型规范测试和后端完整回归测试。

### 实现结果

- `workflow_service.py` 已由约 1285 行缩减到约 980 行，配置解析、Repository 和定义 CRUD 已完成分层。
- 工作流运行和恢复上下文已从无结构字典迁移为实体。
- Redis Checkpointer 不再存在自动内存降级路径。
- Python 语法检查、返回类型规范测试、专项测试和后端完整 65 个测试全部通过。
- 已连接本机 Docker Redis Stack 完成真实初始化验证，返回类型为 `AsyncRedisSaver`。

## 前端工作流动态编译为原生 LangGraph

### work_list

1. 创建 `langchain_test_langgraph` 分支，隔离原生 LangGraph 升级工作。
2. 定义 `NativeWorkflowState`，使用按节点 ID 合并的输出 State 支持并行写入。
3. 将 input、Agent、条件、并行、审批和 output 节点动态注册为 LangGraph 节点。
4. 将条件节点转换为原生 `add_conditional_edges()`。
5. 为并行分支公共汇合点建立原生 barrier，并合并各前驱输出。
6. 将审批节点拆成幂等准备节点和 `interrupt()` 节点，避免恢复时重复写审批记录。
7. 将顺序 steps 配置转换为等价图，统一进入原生编译器。
8. 将运行入口从旧 `_run_workflow_segment → _walk_graph` 切换到 `NativeWorkflowEngine`。
9. 将 Checkpoint thread ID 升级到 `workflow_run_v2_{run_id}`，隔离旧图快照。
10. 增加线性、条件、并行和审批恢复测试并执行完整回归。

### 实现结果

- 工作流运行主链已改为前端节点动态编译的 `CompiledStateGraph`。
- 条件、并行和人工审批均由 LangGraph 原生图调度。
- 旧 `_walk_graph()` 已退出运行主链，暂时仅作为迁移对照代码保留。
- 新增 4 个原生动态图专项测试，后端完整 69 个测试全部通过。
- Python 语法检查、返回类型规范检查和 `git diff --check` 全部通过。
- 当前 Git 分支为 `langchain_test_langgraph`。
