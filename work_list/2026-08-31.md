# 2026-08-31 开发记录

## MongoDB Trace Span 存储升级

### 功能描述

Trace 存储改为混合架构：

- MySQL 保存 `trace_runs` 汇总、状态、用户、Agent、工作流关联、耗时和 Token 等稳定字段。
- MongoDB 保存 LLM 与 Tool 的 `trace_spans` 明细、输入输出和扩展元数据。

根据本次产品要求，MongoDB 是强依赖：连接失败时后端拒绝启动；运行期间 Span 写入失败会明确抛错，不回退写入 MySQL。

### MongoDB 本地部署

- 使用 Docker Compose 安装 MongoDB `8.0.12`。
- 容器名称：`agent-platform-mongodb`。
- 仅监听 `127.0.0.1:27017`，不对局域网公开。
- 使用 Docker Volume `agent-platform-mongodb-data` 持久化数据。
- 配置账号认证、健康检查和自动重启。

管理命令：

```bash
docker compose -f docker-compose.mongodb.yml up -d
docker compose -f docker-compose.mongodb.yml ps
docker compose -f docker-compose.mongodb.yml logs mongodb
docker compose -f docker-compose.mongodb.yml stop
```

### 应用改动

- 新增 MongoDB 连接生命周期管理，应用启动时执行 `ping` 和索引初始化。
- 应用关闭时释放 MongoDB 客户端。
- `TraceContext` 创建和完成 LLM/Tool Span 时改为读写 MongoDB。
- Trace 列表的 `span_count` 使用 MongoDB 聚合查询。
- Trace 详情从 MongoDB 读取 Span，并保持原前端字段结构。
- Trace Run 汇总继续使用 MySQL，避免影响权限关联和报表查询。
- 新增依赖 `motor>=3.6.0`。

### MongoDB 索引

- `trace_run_id + started_at`：Trace 详情调用链查询。
- `user_id + started_at`：用户维度查询。
- `workflow.run_id`：工作流运行关联查询。
- `expire_at` TTL：默认保留 90 天后自动删除。
- `legacy_mysql_id` 唯一稀疏索引：保证历史迁移可以重复执行。

### 历史数据迁移

- 新增幂等迁移脚本：`backend/scripts/migrate_trace_spans_to_mongodb.py`。
- MySQL 历史 Span：184 条。
- 成功迁移到 MongoDB：184 条。
- 迁移后数量核对一致。
- 原 MySQL Span 暂时保留作为迁移备份，应用已经不再读取或写入该表。

迁移命令：

```bash
backend/.venv-langchan/bin/python -m backend.scripts.migrate_trace_spans_to_mongodb
```

### 相关文件

- `docker-compose.mongodb.yml`
- `backend/config.py`
- `backend/mongo_client.py`
- `backend/services/mongo_trace_service.py`
- `backend/core/trace_handler.py`
- `backend/routers/traces.py`
- `backend/main.py`
- `backend/scripts/migrate_trace_spans_to_mongodb.py`
- `backend/requirements.txt`
- `backend/test/test_mongo_trace_service.py`

### 验证情况

- MongoDB 容器健康检查通过。
- MongoDB 写入、更新、读取和删除冒烟测试通过。
- 历史迁移数量核对通过。
- 已确认 Trace 所需索引全部创建。
- Trace #67 的 MongoDB 明细查询与聚合计数一致，共 7 个 Span。
- 后端 30 项单元测试全部通过。
- FastAPI 完整启动与关闭验证通过，启动日志确认 MongoDB 初始化成功。
- `git diff --check` 通过。

## Agent Runtime 实现文档

### 工作内容

新增 `docs/agent-runtime-implementation.md`，按照当前实际代码整理 Agent Runtime 与 Python Tool Runtime 的完整实现。

文档包含：

- Agent Runtime、Tool Runtime、Runtime Worker 和 Sandbox Service 的职责边界。
- FastAPI、Redis、Worker、Sandbox Service 与用户容器的完整调用链。
- 每个核心类、数据模型和函数的作用。
- 一个 Worker、多个 Worker、用户容器和 workspace 的数量关系。
- Redis Key、文件目录、MySQL 数据职责和容器生命周期。
- Docker 沙箱安全限制、启动方式、故障排查和后续升级建议。

相关文件：`docs/agent-runtime-implementation.md`。

## Agent 流式事件与执行过程展示文档

新增 `docs/agent-streaming-events-and-reasoning-display.md`，整理单 Agent 和多 Agent 工作流的 SSE 事件实现。

主要内容：

- 明确执行过程展示不等于模型隐藏 Chain of Thought。
- 说明 `astream_events()` 到平台 SSE 的转换过程。
- 逐项对照聊天端 `thinking/chunk/reclassify/tool_start/tool_end/structured_result/error/done`。
- 逐项对照工作流 `start/node_start/token/branch/parallel/approval/structured_result/done/error`。
- 说明每种事件的产生位置、数据结构、前端处理、页面效果和持久化方式。
- 记录当前事件协议限制以及统一事件、Tool Call ID、脱敏层等升级建议。
